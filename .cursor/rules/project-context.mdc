---
description: NanoClaw 项目架构与开发约定，始终加载
alwaysApply: true
---

# NanoClaw 项目概览

个人 Claude 助手框架。单个 Node.js 进程，基于 skill 的 channel 系统。消息路由到运行在容器（Linux VM）中的 Claude Agent SDK。每个 group 拥有独立的文件系统和记忆。

## 架构

- Channel（WhatsApp、Telegram、Slack、Discord、Gmail）以 skill 形式在启动时自注册
- 收到消息后，主进程启动一个容器，容器内运行 `agent-runner`（`/app/src/index.ts`）
- 每个 group 有独立的 agent-runner 副本，存放在 `data/sessions/{group}/agent-runner-src/`，挂载为可写，Bot 可以修改自己的代码
- 容器每次启动时重新编译 `/app/src/`，改动立即生效

## 关键文件

| 文件 | 作用 |
|------|------|
| `src/index.ts` | 主编排器：状态机、消息循环、agent 调用 |
| `src/channels/registry.ts` | Channel 注册表（启动时自注册） |
| `src/ipc.ts` | IPC 监听与任务处理 |
| `src/router.ts` | 消息格式化与出站路由 |
| `src/config.ts` | 触发词、路径、时间间隔配置 |
| `src/container-runner.ts` | 启动容器并配置挂载点 |
| `src/task-scheduler.ts` | 定时任务调度 |
| `src/db.ts` | SQLite 操作 |
| `container/agent-runner/src/index.ts` | Agent Runner 模板（初始化用） |
| `data/sessions/{group}/agent-runner-src/` | 各 group 专属 agent-runner 代码（Bot 可自改） |
| `groups/{name}/CLAUDE.md` | 各 group 的独立记忆 |

## 容器挂载关系

| 主机路径 | 容器内路径 | 权限 |
|----------|-----------|------|
| `ClawPartner/`（项目根） | `/workspace/project` | 可写（main group）|
| `data/sessions/{group}/agent-runner-src/` | `/app/src` | 可写 |
| `groups/{group}/` | `/workspace/group` | 可写 |
| `data/sessions/{group}/.claude/` | `/home/node/.claude` | 可写 |
| `data/sessions/{group}/ipc/` | `/workspace/ipc` | 可写 |

## 开发命令

```bash
npm run dev          # 热重载开发
npm run build        # 编译 TypeScript
./container/build.sh # 重建 agent 容器镜像
```

服务管理（macOS launchd）：
```bash
launchctl load ~/Library/LaunchAgents/com.nanoclaw.plist
launchctl unload ~/Library/LaunchAgents/com.nanoclaw.plist
launchctl kickstart -k gui/$(id -u)/com.nanoclaw  # 重启
```

## 常见问题

- **容器构建缓存问题**：`--no-cache` 不能清除 COPY 步骤缓存。需要先 `docker builder prune`，再运行 `./container/build.sh`
- **修改主服务代码**：改 `src/` 后需要 `npm run build` 并重启服务才生效
- **修改 agent-runner**：改 `data/sessions/{group}/agent-runner-src/index.ts`，下次容器启动自动生效
